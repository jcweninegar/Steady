Important context before you start: Claude Code has made changes to src/App.jsx and pushed them to the dev branch on GitHub at <github.com/jcweninegar/Steady>. You must pull the latest code from the dev branch on GitHub before making any changes. Do not overwrite or revert anything Claude changed. If you do not pull first, you will create a merge conflict.
Pull from the dev branch on GitHub now, then make these three changes:
	1.	EVENT TRACKING — Add a Supabase events table and write to it.
Create a table called events in Supabase with these columns: user_id (UUID), event (TEXT), ts (TIMESTAMPTZ default now()). Enable RLS — users can only insert their own rows.
Track these events by calling supabase.from(“events”).insert(…) at each point: app_opened on App mount once per session, brain_dump_submitted when a user message is sent in ChatContent, task_confirmed when a task card is added to the plan, plan_viewed when the Plan sheet opens, journal_viewed when the Journal sheet opens, full_loop_completed when a journal entry is generated for a day that also has at least one completed task and at least one brain dump.
Do not block the UI waiting for these writes. Fire and forget.
	2.	TASK CARD COLOR SCHEME FIX — One section of TaskSheet in src/App.jsx uses hardcoded colors instead of the theme T object. Find every instance of a hardcoded hex color or hardcoded rgba value inside TaskSheet and replace them with the correct T.text, T.sub, T.muted, T.card, T.surface, T.border, T.accent, T.divider, T.red, or T.green values from the theme object.
	3.	START TIME FIELD ON TASK CARDS — Add a start time field to TaskSheet alongside the due date field. Use an input of type time. Store the value as startTime on the task object. Sync it to Supabase in the tasks table as a new column called start_time (text). In all task list views across the app, display a small clock icon next to any task that has a startTime set so the user can see at a glance which tasks are appointments.
When done, push your changes to the dev branch on GitHub.